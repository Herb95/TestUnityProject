using System;

namespace Test2
{
    class Test
    {
        // public void SoftItem(){
        //      list = BackPackManager.Instance.CopyItemsList(dates);
        //     List<ItemDate> datesl = new List<ItemDate>();
        //     for (int i = 0; i < list.Count - 1; i++)
        //     {
        //        for (int j = i + 1; j < list.Count; j++)
        //        {
        //            if (list[i].Quality > list[j].Quality)
        //            {
        //                ItemDate temp = new ItemDate();
        //                temp = list[i];
        //                list[i] = list[j];
        //                list[j] = temp;
        //            }
        //            else if (dates[i].Quality == dates[j].Quality)
        //            {
        //                if (list[i].Id < list[j].Id)
        //                {
        //                    ItemDate temp = new ItemDate();
        //                    temp = list[i];
        //                    list[i] = list[j];
        //                    list[j] = temp;
        //                }
        //            }
        //        }
        //        datesl.Add(dates[i]);
        //     }
        // }

    }
}